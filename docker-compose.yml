version: '3.8' 

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - ENVIRONMENT=development
      - PORT=5000
    volumes:
      - ./app:/app/app
      - ./tests:/app/tests
    command: python app/main.py
